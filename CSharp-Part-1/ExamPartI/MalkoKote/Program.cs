using System;

namespace MalkoKote
{
    class Program
    {
        static void Main(string[] args)
        {
            int s = int.Parse(Console.ReadLine());
            char c = (char)Console.Read();

            int pos = (s - 10) / 2;
            int width = 9;
            for (int i = 0; i < pos; i++)
            {
                width++;
            }


            for (int i = 0; i < s; i++)
            {

            }
        }
    }
}
